# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: deploy
'on':
  workflow_dispatch: {}
jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@v4
    - name: Install PHP & tools
      run: 'sudo apt-get update -y

        sudo apt-get install -y php php-cli php-mbstring php-xml php-zip unzip curl

        '
    - name: Install Composer
      run: 'curl -sS https://getcomposer.org/installer | php

        sudo mv composer.phar /usr/local/bin/composer

        '
    - name: Prepare bootstrap cache directory
      run: mkdir -p bootstrap/cache && chmod 777 bootstrap/cache
    - name: Disable Composer advisory block policy
      run: composer config --no-interaction -g policy.advisories.block false
    - name: Install dependencies
      run: composer install --no-interaction --prefer-dist --optimize-autoloader
    - name: Run PHP lint
      run: find app bootstrap config routes -name "*.php" -exec php -l {} \; | grep
        -v "No syntax errors" || true
  test:
    runs-on: ubuntu-latest
    needs:
    - lint
    timeout-minutes: 15
    steps:
    - uses: actions/checkout@v4
    - name: Install PHP & tools
      run: 'sudo apt-get update -y

        sudo apt-get install -y php php-cli php-mbstring php-xml php-zip unzip

        '
    - name: Install Composer
      run: 'curl -sS https://getcomposer.org/installer | php

        sudo mv composer.phar /usr/local/bin/composer

        '
    - name: Prepare bootstrap cache directory
      run: mkdir -p bootstrap/cache && chmod 777 bootstrap/cache
    - name: Disable Composer advisory block policy
      run: composer config --no-interaction -g policy.advisories.block false
    - name: Install dependencies
      run: composer install --no-interaction --prefer-dist --optimize-autoloader
    - name: Copy .env for testing
      run: cp .env.example .env && php artisan key:generate
    - name: Run tests
      run: php artisan test --env=testing
  provision:
    runs-on: ubuntu-latest
    needs:
    - test
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
    timeout-minutes: 30
    outputs:
      instance_ip: ${{ steps.get_ip.outputs.instance_ip }}
    steps:
    - uses: actions/checkout@v4
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
    - name: Terraform Init
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\n"
      working-directory: infra
    - id: tf_apply
      name: Terraform Apply
      run: terraform apply -input=false -auto-approve
      working-directory: infra
    - id: get_ip
      name: Get EC2 IP
      run: 'echo "instance_ip=$(terraform output -raw instance_ip)" >> "$GITHUB_OUTPUT"

        '
      working-directory: infra
  configure:
    runs-on: ubuntu-latest
    needs:
    - provision
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
    timeout-minutes: 20
    steps:
    - uses: actions/checkout@v4
    - name: Install Ansible
      run: pip install ansible-core
    - name: Write SSH key
      run: 'mkdir -p ~/.ssh

        printf ''%s\n'' "$SSH_KEY" > ~/.ssh/deploy_key

        chmod 600 ~/.ssh/deploy_key

        '
      env:
        SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
    - id: get_ip
      name: Get instance IP
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\necho\
        \ \"instance_ip=$(terraform output -raw instance_ip)\" >> \"$GITHUB_OUTPUT\"\
        \n"
      working-directory: infra
    - name: Scan host keys
      run: ssh-keyscan -H "${{ steps.get_ip.outputs.instance_ip }}" >> ~/.ssh/known_hosts
    - name: Generate inventory
      run: 'cat > /tmp/inventory.ini <<EOF

        [web]

        ${{ steps.get_ip.outputs.instance_ip }} ansible_user=${{ secrets.SSH_USER
        }} ansible_ssh_private_key_file=~/.ssh/deploy_key

        EOF

        '
    - name: Run Ansible playbook
      run: "ansible-playbook -i /tmp/inventory.ini ansible/site.yml \\\n  -e \"app_key=${{\
        \ secrets.APP_KEY }}\" \\\n  -e \"app_env=production\"\n"
      env:
        APP_KEY: ${{ secrets.APP_KEY }}
        APP_ENV: production
  verify:
    runs-on: ubuntu-latest
    needs:
    - configure
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@v4
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
    - id: get_ip
      name: Get instance IP
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\necho\
        \ \"instance_ip=$(terraform output -raw instance_ip)\" >> \"$GITHUB_OUTPUT\"\
        \n"
      working-directory: infra
    - name: Health check
      run: "curl --fail --retry 10 --retry-delay 15 --retry-all-errors \\\n  http://${{\
        \ steps.get_ip.outputs.instance_ip }}/\n"
